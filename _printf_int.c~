#include "main.h"
/**
 * _putchar - a function 
 *Return:
 */
int _putchar(char so)
{
return(write(1, &so, 1));
}

void _case_i_d_int (Famous)
{
  int raqm = va_arg(Famous, int);
  int orko = 1;
  if (raqm < 0)
    {
      _putchar('-')
	finally_yes++;
      raqm *= -1;
    }
  while ((raqm / orko) > 9)
    ra


int _printf(const char *format, ...);
